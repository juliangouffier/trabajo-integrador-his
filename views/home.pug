doctype html
html
  head
    title= title
    link(rel='stylesheet', href='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css')
  body
    .container.mt-5
      h1.text-center.mb-4 Sistema de Información Hospitalaria
      .row.g-4
        each item in items
          .col-md-4
            a(href=item.link class='card text-decoration-none text-dark')
              .card-body
                h5.card-title.text-center= item.title
                p.card-text.text-center= item.desc
    script(src='https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js')
